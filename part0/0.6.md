sequenceDiagram
    participant browser
    participant server

    browser->>server: POST /new_note_spa (JSON: content + date)
    server-->>browser: 201 Created

    browser-->>server: Updates UI without reloading page
